using System;
using System.Collections.Generic;
using System.IO;
using System.Linq;

namespace codeeval.moderate
{
    public class SimpleSorting
    {
        //private static void Main(string[] args)
        //{
        //    foreach (var nums in File.ReadAllLines(args[0]).Select(x => x.Split(' ')))
        //    {
        //        List<double> _nums = nums.Select(double.Parse).ToList();
        //        _nums.Sort();
        //        var list = new List<string>(_nums.Count);
        //        foreach (var number in _nums)
        //        {
        //            Console.Write("{0:F3} ", number);
        //        }
        //        Console.WriteLine();
        //        //list.AddRange(_nums.Select(d => d.ToString("#.000")));
        //        //Console.WriteLine(string.Join(" ", list));
        //    }
        //}
    }
}
