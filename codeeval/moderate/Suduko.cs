using System;
using System.IO;
using System.Linq;

namespace codeeval.moderate
{
    public class Suduko
    {
        //private static void Main(string[] args)
        //{
        //    foreach (var result in File.ReadAllLines(args[0]).Select(x => x.Split(';')))
        //    {
        //        int n = int.Parse(result[0]);
        //        var a = result[1].Split(',').Select(int.Parse).ToArray();
        //        bool yes = true;
        //        for (int i = 0; i < n; i++)
        //        {
        //            int[] numsx = new int[n];
        //            int[] numsy = new int[n];
        //            for (int j = 0; j < n; j++)
        //            {
        //                numsx[a[(i*n) + j] - 1]++;
        //                numsy[a[(j*n) + i] - 1]++;
        //            }
        //            if (!numsx.All(x => x == 1) || !numsy.All(x => x == 1))
        //            {
        //                yes = false;
        //                break;
        //            }
        //        }
        //        Console.WriteLine(yes);
        //    }
        //}
    }
}
