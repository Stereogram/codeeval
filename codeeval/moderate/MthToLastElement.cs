using System;
using System.IO;
using System.Linq;

namespace codeeval.moderate
{
    public class MthToLastElement
    {
        //static void Main(string[] args)
        //{
        //    foreach (var line in File.ReadAllLines(args[0]).Select(x => x.Split(' ')))
        //    {
        //        var a = line.Reverse().ToArray();
        //        int n = int.Parse(a[0]);
        //        if(n < line.Count())
        //            Console.WriteLine(a[n]);
        //    }
        //}
    }
}
