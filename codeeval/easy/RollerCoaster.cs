using System;
using System.IO;
using System.Linq;

namespace codeeval.easy
{
    public class RollerCoaster
    {
        //private static void Main(string[] args)
        //{
        //    foreach (var line in File.ReadAllLines(args[0]))
        //    {
        //        bool b = false;
        //        var s = line.Aggregate(string.Empty, (c, n) =>
        //        {
        //            c += b ? char.ToLower(n) : char.ToUpper(n);
        //            if(char.IsLetter(n)) b = !b;
        //            return c;
        //        });
        //        Console.WriteLine(s);
        //    }
        //}
    }
}
