using System;
using System.IO;
using System.Linq;

namespace codeeval.easy
{
    public class RightmostChar
    {
        //static void Main(string[] args)
        //{
        //    foreach (var result in File.ReadAllLines(args[0]).Select(x => x.Split(',')))
        //    {
        //        Console.WriteLine(Array.LastIndexOf(result[0].ToCharArray(), char.Parse(result[1])));
        //    }
        //}
    }
}
