using System;
using System.Collections.Generic;
using System.IO;
using System.Linq;

namespace codeeval.easy
{
    public class RoadTrip
    {
        //static void Main(string[] args)
        //{
        //    foreach (List<int> test in File.ReadAllLines(args[0]).Select(x=>x.Split(';')).Select(word => word.TakeWhile(str => str != string.Empty).Select(str => int.Parse(str.Split(',').Last())).ToList()))
        //    {
        //        test.Sort();
        //        int sum = 0;
        //        List<int> arr = new List<int>();
        //        foreach (int t in test)
        //        {
        //            arr.Add(t-sum);
        //            sum += t;
        //        }
        //        Console.WriteLine(string.Join(",",arr));
        //    }
        //}
    }
}
