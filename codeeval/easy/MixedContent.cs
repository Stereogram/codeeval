using System;
using System.Collections.Generic;
using System.IO;
using System.Linq;

namespace codeeval.easy
{
    public class MixedContent
    {
        //static void Main(string[] args)
        //{
        //    foreach (var result in File.ReadAllLines(args[0]))
        //    {
        //        var nums = new List<double>();
        //        var words = new List<string>();
        //        foreach (var word in result.Split(','))
        //        {
        //            double a = 0;
        //            if (double.TryParse(word, out a))
        //                nums.Add(a);
        //            else
        //                words.Add(word);
        //        }
        //        Console.WriteLine(string.Join(",",words)+"|"+string.Join(",",nums));
        //    }
        //}
    }
}