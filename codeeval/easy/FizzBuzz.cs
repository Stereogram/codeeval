using System;
using System.IO;
using System.Linq;

namespace codeeval.easy
{
    public class FizzBuzz
    {
        //private static void Main(string[] args)
        //{
        //    foreach (var input in File.ReadAllLines(args[0]).Select(x => x.Split(' ')))
        //    {
        //        //First divider, replace with "fizz" (or F).
        //        int X = int.Parse(input[0]);
        //        //Second divider replace with "buzz" (or B).
        //        int Y = int.Parse(input[1]);
        //        //How high we count to.
        //        int N = int.Parse(input[2]);
        //        for (int i = 1; i <= N; i++)
        //        {
        //            if (i % X == 0)
        //                Console.Write("F");
        //            if (i % Y == 0)
        //                Console.Write("B");
        //            if (i % X != 0 && i % Y != 0)
        //                Console.Write(i);
        //            Console.Write(" ");
        //        }
        //        Console.WriteLine();
        //    }
        //}

    }
}
