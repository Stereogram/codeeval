using System;
using System.IO;
using System.Linq;

namespace codeeval.easy
{
    public class FizzBuzz
    {
        //static void Main(string[] args)
        //{
        //    foreach (var r in File.ReadAllLines(args[0]).Select(x => x.Split(' ')))
        //    {
        //        int a = int.Parse(r[0]);
        //        int b = int.Parse(r[1]);
        //        for (int i = 1; i <= int.Parse(r[2]); i++)
        //        {
        //            if(i % a == 0)
        //                Console.Write("F");
        //            if (i % b == 0)
        //                Console.Write("B");
        //            if(i % a != 0 && i % b != 0)
        //                Console.Write(i);
        //            Console.Write(" ");
        //        }
        //        Console.WriteLine();
        //    }
        //}
    }
}
