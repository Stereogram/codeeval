using System;
using System.Collections.Generic;
using System.IO;
using System.Linq;

namespace codeeval.easy
{
    public class DateRecovery
    {
        //private static void Main(string[] args)
        //{
        //    foreach (var list in File.ReadAllLines(args[0]).Select(x => x.Split(';')))
        //    {
        //        var a = list[0].Split(' ');
        //        var b = list[1].Split(' ').Select(int.Parse).ToArray();
        //        var d = new Dictionary<int, int>();
        //        for (int i = 0; i < b.Count(); i++)
        //        {
        //            d.Add(b[i],i);
        //        }
        //        string[] c = a.Clone() as string[];
        //        for (int i = 1; i <= b.Count(); i++)
        //        {
        //            if(d.ContainsKey(i))
        //                c[i-1] = a[d[i]];
        //        }
        //        Console.WriteLine(string.Join(" ", c));
        //    }
        //}
    }
}
